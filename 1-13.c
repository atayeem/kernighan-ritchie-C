// Create a histogram of the lengths of words in the input

#include <stdio.h>

int main(void)
{
    int c;
    int lens[32];
    int len = 0;
    
    while ((c = getchar()) != EOF)
    {
        switch (c)
        {
            case ' ':

        }
    }
}